// Replace eve's built-in web_fetch with Nimble Extract: rendered pages,
// cleaned markdown main content, plus the links found on the page.
export { extract as default } from "@nimble-way/eve/tools";
